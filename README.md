# Weather App (Python CLI)

A simple command-line weather application that fetches real-time weather data using the OpenWeather API. Enter a city name and instantly get temperature, humidity, and weather conditions.

## Features
- Real-time weather data
- Clean CLI interface
- Error handling for invalid cities or network issues
- Uses the OpenWeather API

## How to Run
1. Install dependencies:
